# google-reviews-scraper
A Python-based Google Maps reviews scraper that extracts reviews, ratings, reviewer names, dates, and Google’s default review questions. Results are saved as JSON, with optional SQLite database storage.
